class Example {
  sum(a, b) {
    return a+b;
  }

  multiply(a,b) {
    return a*b;
  }

  divide(a,b) {
    return a/b;
  }

  substract(a,b) {
    return a-b;
  }

  pow(a,b) {
    return Math.pow(a,b);
  }
}

export default Example;
