class Example {
  sum(a, b) {
    //This should be corrected before saving!
    return a+b;
  }

  multiply(a,b) {
    return a*b;
  }

  divide(a,b) {
    return a/b;
  }
  substract(a,b) {

  }
}

export default Example;
